# cloudflare-solver の依存関係と同じものを使用
browserforge==1.2.3
camoufox==0.4.11
playwright==1.52.0

# HTTP クライアント (OCR API 呼び出し用)
httpx>=0.27.0

# FreeDNS アカウント生成
freedns-client>=0.1.4

# 使い捨てメール
tmailor>=0.0.12
